
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sbikash010/microserviceapi
          clone-on-start: true
#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultZone: ${DISCOVERY_SERVICE:http://localhost:8761/eureka/}

server:
  port: 1116